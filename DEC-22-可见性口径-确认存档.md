# DEC-22 · 知识库可见性口径 · 确认存档

> **状态：✅ 已确认并冻结 · 2026-09-16**
> 本文件原为「待填确认表」，现已转为**确认存档**（决策的可追溯凭据）。
> 契约侧已同步：`AI-Coding-Agent-决策契约.md` §1.1 YAML、§2・DEC-22、§3 全节、§3.8 确认记录、§4 `decisions.py`。
>
> **本项冻结后，`HARD-BLOCK` 未决项归零** —— 步骤 0~16 全部解锁，Agent 可一路推进到里程碑 B，不再需要回问人类。

---

## 0. 最终答复（原文）

```
DEC-22 按推荐方案执行：grant_kb 作主闸门（必要条件）+ grant_tag 作文档级细化
+ grant_org 作标签来源。9 个问题全部取推荐值，7 条用例全部确认，映射表暂不填。
Q2：有全员可见的公开库
Q3 · 上级部门能看到下级部门的文档
Q9 · 新账号只能看见公开库
```

**三点需要说明**

| 点 | 说明 |
|---|---|
| 骨架按推荐 | `grant_kb`（必要条件）+ `grant_tag`；`grant_org` **不参与判定**，只作标签来源 |
| Q2 / Q9 与推荐值一致 | 这两条**就是**推荐值（「有公开库」「只能看见公开库」），未产生偏离 |
| **Q3 是唯一偏离项** | 推荐值为「不能」，人工指定为「**能**」→ 触发了机制变更，详见第 3 节 |

---

## 1. 口径骨架（已确认）

| 口径 | 是否启用 | 在判定中的角色 |
|---|---|---|
| `grant_kb` | ✅ 启用 | **主闸门 / 必要条件**（闸门 G2） |
| `grant_tag` | ✅ 启用 | 文档级细化（闸门 G4） |
| `grant_org` | ✅ 启用 | **仅作标签来源** —— 组织架构产出 `dept:` 标签，自身不参与判定 |

```
grant_kb   作为主闸门（必要条件）    ← 先决定"这个人能不能用这个知识库"
grant_tag  作文档级细化             ← 再决定"知识库里哪些文档他看得见"
grant_org  作为 grant_tag 的标签来源 ← 部门路径成为可匹配的标签
```

**为什么知识库成员必须是必要条件而不是或条件**

> 一份标了 `dept:财务` 的文档，被误放进了任何人可访问的公共知识库。
> - 若是 **OR** → 研发同事因为知识库公开就搜到了财务文档 → **泄漏**
> - 若是 **AND** → 他搜不到 → **安全**
>
> **⚠️ 但 AND 只在标签是纯的时候成立。** 若该文档 `acl_tags = {public, dept:财务}`，G4 对全体用户恒真，G2 就成了唯一防线 —— 而 Q2 又让公开库对所有用户成立。
> **所以 `public` 必须是互斥标签**（契约 §3.3(3)）。这是 AND 规则成立的前提，**原设计漏了**。

---

## 2. 九个问题（逐条归档）

| # | 问题 | 最终答复 | 与推荐值 |
|---|---|---|---|
| Q1 | `kb_members` 支持哪些成员类型 | 用户 + 部门 + 用户组 + 角色（四种） | 同推荐 |
| Q2 | 是否存在「全员可见」的知识库 | **有**，且只建一个（`is_public=true`），所有用户自动视为成员 | 同推荐 |
| Q3 | 上级部门能否看到下级部门的文档 | **能** | ⚠️ **偏离推荐值「不能」** |
| Q4 | 是否支持兼岗（主部门 + 兼岗） | 支持，`user_subjects` 取**并集** | 同推荐 |
| Q5 | 文档的部门归属标签由谁决定 | 上传者所属部门**自动打标**，上传时可手动覆盖 | 同推荐 |
| Q6 | 跨部门文档（同时标两个部门） | **任一命中即可见** | 同推荐 |
| Q7 | 是否要「显式拒绝」（`doc_acl.deny`） | 要（合规调查临时收回 / 离职冻结 / 外包到期收回） | 同推荐 |
| Q8 | 外部人员（外包 / 顾问 / 实习生） | 独立用户组 `group:external_*` + **显式授权**，**不给部门标签** | 同推荐 |
| Q9 | 新账号（未分配部门）默认可见范围 | 只能看见公开库中 `acl_tags` 含 `public` 的文档 | 同推荐 |

**未填项**

| 项 | 状态 | 影响 |
|---|---|---|
| 公开知识库名称 / 维护人 | ⬜ 未填 | 不阻塞开发；上线前需指定（运维信息） |
| Q4 兼岗占比 | ⬜ 未填 | 不阻塞 |
| Q7 实际 deny 场景清单 / 后台操作人 | ⬜ 未填 | 不阻塞开发；**上线前 MUST 指定责任人** |
| Q8 是否已有外部人员 | ⬜ 未填 | 不阻塞 |

---

## 3. ★ Q3 的机制变更（本项唯一需要改设计的地方）

### 3.1 变更内容

| 项 | 变更前 | 变更后（MUST） |
|---|---|---|
| 用户主体 · 上向（祖先） | ✅ 有 | ✅ 有（不变） |
| 用户主体 · 下向（子孙） | ❌ 无 | ✅ **新增，传递闭包** |
| 文档标签 | ❌ 祖先 ∪ 自己 | ✅ **仅自己** |
| 上级看下级 | ⚠️ 偶然可见（因双方都含根节点） | ✅ 精确定义（用例 8） |
| **同级（兄弟部门）** | ❌ 可见 | ✅ **不可见**（用例 9） |
| **跨分支（研发 vs 财务）** | ❌ **可见（真实泄漏）** | ✅ 不可见（用例 10） |

### 3.2 为什么变更前的写法是泄漏而不是"略宽"

```
user@dept:研发     subjects = {public, dept:公司, dept:研发}
doc  标 dept:财务  tags     = {public, dept:公司, dept:财务}
                                    ↑ 交集非空（共同祖先 dept:公司）→ 研发同事看到财务文档 ✗✗
```

**组织中任意两个主体最终必然共享根节点。** 一旦文档标签也展开祖先，
"交集非空即可见"就退化为**全公司互通** —— 这是匹配模型的**固有性质**，不能靠调参或加注释规避。

因此：

| 侧 | 展开 | 理由 |
|---|---|---|
| 用户主体 | 祖先 ∪ 自己 ∪ 子孙 | 实现「下级看上级」+「上级看下级」 |
| 文档标签 | **仅自己所属的那一条路径** | 一旦展开就全公司互通 |

> `dept:` 前缀的语义就此固定为：**用户侧是"我能看到的部门集合"，文档侧是"我归属的部门"**。
> **两侧语义不同是有意的，不是不对称缺陷** —— 对称才是漏洞。

### 3.3 净效果

Q3 的表面代价是"放宽了上级可见性"，**实际净效果是收紧**：同级与跨分支从"可见"变为"不可见"。
三个方向的调整都在契约 §3.3 与 §3.5 用例 3b / 8 / 9 / 10 中固化为断言。

---

## 4. ★ 落盘过程中新发现并修掉的三处缺陷

不需要新的人工决策 —— 它们都是「口径已确认、**实现曾经是错的**」类型。

| # | 缺陷 | 后果 | 修法 | 守它的用例 |
|---|---|---|---|---|
| ① | 文档标签做祖先展开 | 共享根节点 → **全公司互通** | `DOC_TAG_ANCESTOR_EXPANSION = False` + `enforce()` 断言 | 用例 10 |
| ② | `{public, dept:x}` 混合标签 | G4 对全员恒真，只剩 G2 在守；配合公开库即泄漏 | `ACL_PUBLIC_MUTEX` + 写库断言 | 用例 3b |
| ③ | `acl_tags` 含 `level:` 标签 | 与用户主体的 `level:<n>` 组合 → **G4 绕过 G1 的密级比较** | `ACL_LEVEL_TAGS_ALLOWED = False` | 用例 5 |

**还有两处跨文档不一致，一并修正**

| 不一致 | 原状态 | 修正 |
|---|---|---|
| G2 可被 `visibility='public'` 绕过 | 实施规范 §3.1.2 / §6.3 把 `visibility='public'` 直接判为"有权" | 提为**前置必要条件 P1~P3**（G1/G2 为 AND，不可被后续规则救回） |
| `level` 与 `level_rank` 不同标尺 | 一处 `0..3`，一处 `10/20/30/40` | 明确 `level_rank = (level + 1) * 10`，检索下推 MUST 用 rank |

> **③ 与"G2 可被绕过"这两条不属于本次口径变更范围**，但同属 G4/G2 的判定链路 —— 既然在核对口径时看到了，就一起修了，否则会留下一批"口径正确、实现错误、测试全绿"的代码。

---

## 5. 十条验证用例（全部确认）

> 这十条就是"口径确认"的可执行形式，会成为 `tests/security/test_acl_matrix.py`。
> **3b / 8 / 9 / 10 为本次新增** —— 它们守的都是「口径对、实现错」的泄漏面。

| # | 用户属性 | 文档属性 | 期望 | 防的是什么 |
|---|---|---|---|---|
| 1 | 财务部员工 u100，clearance=2 | 财务制度库（u100 成员）、level=内部(20)、`{dept:财务}` | ✅ 可见 | 正常路径，四道闸门全过 |
| 2 | 同上 | 公共制度库（`is_public=true`）、`{public}` | ✅ 可见 | 公开库 + 互斥 public 标签 |
| 3 | 研发部 u200，clearance=2 | 财务制度库（**u200 非成员**）、`{dept:财务}` | ❌ 不可见 | **G2 必要条件** |
| **3b** | 同上 u200 | **公开制度库**、`{dept:财务}`（误放进公开库的财务文档） | ❌ 不可见 | ★ 公开库让 G2 失效，此时只剩 G4 在守 |
| 4 | 财务部 u100 | 有权库、`deny_subjects={user:u100}` 未过期 | ❌ 不可见 | **G3 优先**（调岗 / 离职 / 合规调查） |
| 5 | 财务总监 u100，clearance=3 | level=**机密(40)**，库由 u100 创建 | ❌ 不可见 | **G1 优先**，创建者身份不越过密级 |
| 6 | `dept:财务/资金组` 员工 | `{dept:财务}` | ✅ 可见 | **A1 生效**（下级看上级） |
| 7 | 新账号（无任何归属），clearance=1 | (a) 带部门标签的文档；(b) 公开库中 `{public}` | (a) ❌ (b) ✅ | **fail-closed + Q9** |
| **8** | `dept:财务` 员工 | `{dept:财务/资金组}` | ✅ 可见 | ★ **A2 生效**（上级看下级，Q3 = 能） |
| **9** | `dept:财务/资金组` 员工 | `{dept:财务/核算组}`（同级兄弟） | ❌ 不可见 | ★ **A2 只向下、不横向** |
| **10** | 研发部员工 | `{dept:财务}`（若文档侧误展开祖先则含 `dept:公司`） | ❌ 不可见 | ★★ **防全公司互通** |

**第 7 条是最容易被漏掉的负向用例** —— 很多实现在"用户没有任何 subject"时会因为空集合运算的边界（`∅ ∩ X = ∅` 恰好正确，但 `NOT (∅ ∩ X ≠ ∅)` 恒为真）而**误放行**。必须显式测试。

---

## 6. 「主体属性 → 可见范围」映射表

**⬜ 未填**（人工明确"暂不填"，**不构成阻塞**）

> 契约把这张表列为 DEC-22 的必交付物。它不阻塞开发 —— 但**建议在里程碑 B 之前补上**。
>
> **为什么值得补**：这类规则**几乎总是在"举个真实的人"时才暴露问题**。抽象规则"部门及下级可见"看起来无懈可击，
> 一旦写成"财务部总监 → 能看财务部全部文档"，就会有人提出"那他能不能看到资金组的供应商银行账号" ——
> 这才是真正需要拍板的地方。

**要填的时候，格式如下**（参考行即原表示例，可直接照抄后改）：

| # | 谁（真实岗位） | 密级 | 能进哪些知识库 | 库里能看哪些标签 | 期望能搜到一个真实问题 |
|---|---|---|---|---|---|
| 1 | 财务部-资金组-张三 | 内部 | 财务制度库、公共库 | `dept:财务`、`dept:财务/资金组`、`public` | 「差旅住宿标准是多少」 |
| 2 | 财务部总监 | 秘密 | 财务制度库、经营分析库、公共库 | `dept:财务`、`role:finance_manager`、`public` | 「本季度预算执行率」 |
| 3 | 研发部-普通员工 | 内部 | 研发知识库、公共库 | `dept:研发`、`public` | 「代码评审规范」 |
| 4 | 新入职未分配 | 公开 | 仅公共库 | 仅 `public` | 「年假怎么请」 |
| 5 | 外部顾问 | 内部 | 仅项目专项库 | 仅 `group:external_xxx` 显式授权 | 「项目 A 的技术方案」 |

**填写：**

```
1. ____________ | ____ | ____________________ | __________________________ | ____________________
2. ____________ | ____ | ____________________ | __________________________ | ____________________
3. ____________ | ____ | ____________________ | __________________________ | ____________________
4. ____________ | ____ | ____________________ | __________________________ | ____________________
5. ____________ | ____ | ____________________ | __________________________ | ____________________
```

> 第 5 行（外部顾问）如目前没有外部人员，写「不适用」即可。
> 补填后 **MUST 同步回** `AI-Coding-Agent-决策契约.md` §3.8 的确认记录表。

---

## 7. 完整判定式（已冻结，Agent 照此实现）

```
visible(doc, user) :=
      doc.deleted_at IS NULL
  AND doc.is_latest = true
  AND doc.level_rank   <= user.clearance                    -- 闸门 G1 · 密级
  AND doc.kb_id        IN user.authorized_kb_ids            -- 闸门 G2 · 知识库（必要条件）
  AND NOT (doc.deny_subjects ∩ user.subjects ≠ ∅)           -- 闸门 G3 · 显式拒绝（优先）
  AND (doc.acl_tags    ∩ user.subjects ≠ ∅)                 -- 闸门 G4 · 标签匹配
```

**四条硬规则**

| # | 规则 | 理由 |
|---|---|---|
| 1 | **G3（deny）优先级最高** —— 命中即不可见 | 调岗、离职、合规调查期间的临时封禁必须立即生效 |
| 2 | **闸门之间是 AND，不是 OR** | G2 与 G4 若是 OR，一份误标 `public` 的财务文档放进公开库就泄漏 |
| 3 | **空 `acl_tags` ≠ "对所有人可见"** | 空标签在入库时规范化为 `{public}`，而 `public` 仍是常规主体，仍须过 G2 |
| 4 | **★ 文档标签 MUST NOT 做祖先展开** | 做错不是"多看见一点"，而是**全公司互通**（第 3.2 节） |

**标签书写规范**

```
dept:<路径>          文档自身所属的部门路径（★ 不展开祖先）
user:<主体 id>       具体用户
group:<用户组 id>    用户组
role:<角色>          角色
region:<区域>        区域
public               所有人（含新账号）★ 互斥，不得与其他标签共存
```

**`level:` MUST NOT 出现在 `acl_tags` 中** —— 密级只通过 G1 的数值比较表达。

---

## 8. 该项该找谁定（归档）

| 内容 | 责任人 | 状态 |
|---|---|---|
| 口径骨架（第 1 节） | 业务负责人 + 技术负责人 | ✅ 已确认 |
| Q2 公开库 / Q7 deny 场景 / Q8 外部人员 | 安全合规 | ✅ 已确认（Q7 运维责任人待指定） |
| Q3 上下级可见方向 / Q4 兼岗 | 业务负责人 | ✅ 已确认（**Q3 的扩张尚未与安全合规则单独会签**） |
| Q5 打标方式 / Q6 跨部门 | 技术负责人 + 知识库运营者 | ✅ 已确认（默认值） |

**未会签项（不阻塞，但 MUST 在里程碑 B 之前补）**

| 项 | 该找谁 | 理由 |
|---|---|---|
| Q3 的**可见性扩张**风险确认 | 安全合规 | Q3 = 能 是本次唯一的放宽方向。业务口径已确认，但"上级能看到整个子树的文档"在敏感部门（财务 / 法务 / 人事）可能超出预期 |
| 第 6 节映射表（5 行真实例子） | 各业务线 | 规则几乎总在"举个真实的人"时才暴露问题 |
| Q7 的 deny 运维责任人 | 安全合规 | 谁有权按下这个开关，必须有人负责 |

> 未会签项**不免除任何实现义务** —— 口径已冻结，Agent MUST 按契约 §3 实现。
> 它们只影响「上线前的合规签字」，不影响「现在能不能开工」。

---

## 9. 落盘动作清单（已全部完成）

| # | 动作 | 产物 |
|---|---|---|
| 1 | 契约 §1.1 YAML | `status: frozen`；`value` + `answers`(Q1~Q9) + `frozen_consequences`(8 条 MUST) + `unconfirmed_followups` |
| 2 | 契约 §1.2 / §1.3 / §0.1 / §0.3 | 状态改为已确认，硬阻塞清零 |
| 3 | 契约 §2・DEC-22 | 改为「已冻结」，含 9 分叉答复表、Q3 机制变更对照表、原 BLOCKER 存档 |
| 4 | 契约 §3 全节 | 判定式 4 条硬规则、展开规则 A1/A2/B/C、10 条用例、函数签名、规模核验、缓存失效面 |
| 5 | 契约 §3.8 | 确认记录已填写，含 3 项未会签项 |
| 6 | 契约 §4 `decisions.py` | `VISIBILITY_GRANTS = {KB, TAG}`；新增 7 个常量；`UNRESOLVED` 移除 DEC-22；`enforce()` 加 4 条防回归断言 |
| 7 | 契约 §5.3 / §6 / §7 / §8 / 附录 A / 版本 | 全部同步；版本 `dc-1.1` → **`dc-1.2`** |
| 8 | 实施规范 §3.1 / §3.1.1 / §3.1.2 / §3.2.1 / §3.2.2 / §3.3 / §6.3 / WU-19 | 数据契约与接口契约同步（含 5 处跨文档不一致修正） |
| 9 | 自检 | `scripts/check_decisions.py` 新增 14 项 DEC-22 断言 + 硬阻塞清零断言 |

**数据契约新增（需 alembic 迁移）**

```sql
ALTER TABLE knowledge_bases ADD COLUMN is_public BOOLEAN NOT NULL DEFAULT false;
CREATE UNIQUE INDEX uq_kb_single_public ON knowledge_bases ((is_public)) WHERE is_public;
-- 上面这条部分唯一索引保证：全局 is_public = true 的行最多 1 条
```

**仍待办（不阻塞开发）**

| # | 事项 | 时点 |
|---|---|---|
| 1 | 第 6 节映射表补填 | 里程碑 B 之前 |
| 2 | Q3 可见性扩张与安全合规则会签 | 里程碑 B 之前 |
| 3 | Q7 deny 运维责任人指定 | 上线前 |
| 4 | §3.3 末的**展开规模核验**（跑那条 SQL） | **步骤 4 之前** ← 唯一有时点压力的 |
| 5 | §3.8 函数签名落成可跑骨架 + 10 条用例落成测试文件 | 步骤 9（WU-19） |

---

*关联文档：`AI-Coding-Agent-决策契约.md` §2・DEC-22 / §3（全节）/ §3.8 · `AI-Coding-Agent-实施规范.md` WU-06 / WU-08 / WU-19 / WU-21 / WU-23 · `企业知识库问答AI-Agent-分步开发路线.md` 步骤 4 / 9*
