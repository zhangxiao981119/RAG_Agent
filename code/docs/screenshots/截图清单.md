# 截图清单（补录后删除本文件）

README 首屏引用了以下两个文件，录好后放到本目录，再提交推送：

| 文件名 | 内容要求 | 录制方式 |
|--------|----------|----------|
| `demo-chat.gif` | 提问后 Token 级打字机输出 + 点击 `[1]` 引用角标回跳原文片段的完整过程（10-20 秒） | ScreenToGif（免费）录屏导出 GIF，宽约 900px |
| `admin-panels.png` | admin 登录后的管理面板（建议配额管理或系统监控页，能看到进度条/延迟图表） | Win+Shift+S 截图，宽约 1200px |

录完提交：

```bash
cd code/docs/screenshots
# 放入 demo-chat.gif 和 admin-panels.png，删除本说明文件
git add .
git commit -m "docs: add demo screenshots"
git push
```
